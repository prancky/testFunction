// Auto-generated by app-studio-generator.
import React from 'react';
import { render } from '@testing-library/react-native';
import { describeComponentContract } from '../helpers/component-contract';
import ASPasswordTextField from '../../src/components/ASPasswordTextField';

describeComponentContract(
  'ASPasswordTextField',
  (baseProps) => <ASPasswordTextField {...baseProps}  />,
  (baseProps) => {
  it('renders with secureTextEntry', () => {
    expect(() =>
      render(<ASPasswordTextField {...baseProps}  secureTextEntry={true} />)
    ).not.toThrow();
  });

  it('renders editable={false} variant', () => {
    expect(() =>
      render(<ASPasswordTextField {...baseProps}  editable={false} />)
    ).not.toThrow();
  });

  it('renders with multiline={true}', () => {
    expect(() =>
      render(<ASPasswordTextField {...baseProps}  multiline={true} />)
    ).not.toThrow();
  });

  it('renders with placeholder', () => {
    expect(() =>
      render(<ASPasswordTextField {...baseProps}  placeholder="enter value" />)
    ).not.toThrow();
  });

  it('renders with autoFocus={true}', () => {
    expect(() =>
      render(<ASPasswordTextField {...baseProps}  autoFocus={true} />)
    ).not.toThrow();
  });

  it('renders with customFlex layout', () => {
    expect(() =>
      render(<ASPasswordTextField {...baseProps}  customFlex={1} />)
    ).not.toThrow();
  });

  it('renders with customWidth layout', () => {
    expect(() =>
      render(<ASPasswordTextField {...baseProps}  customWidth={200} />)
    ).not.toThrow();
  });

  it('renders with customAlignSelf layout', () => {
    expect(() =>
      render(<ASPasswordTextField {...baseProps}  customAlignSelf="center" />)
    ).not.toThrow();
  });

  it('renders with customMinWidth layout', () => {
    expect(() =>
      render(<ASPasswordTextField {...baseProps}  customMinWidth={80} />)
    ).not.toThrow();
  });

  it('renders with defaultValue when field is empty', () => {
    expect(() =>
      render(<ASPasswordTextField {...baseProps}  defaultValue="seed" />)
    ).not.toThrow();
  });

  it('renders with maxLength constraint', () => {
    expect(() =>
      render(<ASPasswordTextField {...baseProps}  maxLength={50} />)
    ).not.toThrow();
  });

  }
);
