// Auto-generated by app-studio-generator.
import React from 'react';
import { render } from '@testing-library/react-native';
import { describeComponentContract } from '../helpers/component-contract';
import ASRadioButton from '../../src/components/ASRadioButton';

describeComponentContract(
  'ASRadioButton',
  (baseProps) => <ASRadioButton {...baseProps}  />,
  (baseProps) => {
  it('renders with value={true} (checked state)', () => {
    expect(() =>
      render(<ASRadioButton {...baseProps}  value={true} />)
    ).not.toThrow();
  });

  it('renders with disabled={true} (greyed variant)', () => {
    expect(() =>
      render(<ASRadioButton {...baseProps}  disabled={true} />)
    ).not.toThrow();
  });

  it('renders with populated options array', () => {
    const fixture = [
      { label: 'Option A', value: 'a' },
      { label: 'Option B', value: 'b' },
    ];
    expect(() =>
      render(
        <ASRadioButton
          {...baseProps}
          options={fixture}
          data={fixture}
          items={fixture}
        />
      )
    ).not.toThrow();
  });

  }
);
