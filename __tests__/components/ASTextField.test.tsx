// Auto-generated by app-studio-generator.
import React from 'react';
import { render } from '@testing-library/react-native';
import { describeComponentContract } from '../helpers/component-contract';
import ASTextField from '../../src/components/ASTextField';

describeComponentContract(
  'ASTextField',
  (baseProps) => <ASTextField {...baseProps}  />,
  (baseProps) => {
  it('renders editable={false} variant', () => {
    expect(() =>
      render(<ASTextField {...baseProps}  editable={false} />)
    ).not.toThrow();
  });

  it('renders with multiline={true}', () => {
    expect(() =>
      render(<ASTextField {...baseProps}  multiline={true} />)
    ).not.toThrow();
  });

  it('renders with placeholder', () => {
    expect(() =>
      render(<ASTextField {...baseProps}  placeholder="enter value" />)
    ).not.toThrow();
  });

  it('renders with autoFocus={true}', () => {
    expect(() =>
      render(<ASTextField {...baseProps}  autoFocus={true} />)
    ).not.toThrow();
  });

  it('renders with customFlex layout', () => {
    expect(() =>
      render(<ASTextField {...baseProps}  customFlex={1} />)
    ).not.toThrow();
  });

  it('renders with customWidth layout', () => {
    expect(() =>
      render(<ASTextField {...baseProps}  customWidth={200} />)
    ).not.toThrow();
  });

  it('renders with customAlignSelf layout', () => {
    expect(() =>
      render(<ASTextField {...baseProps}  customAlignSelf="center" />)
    ).not.toThrow();
  });

  it('renders with customMinWidth layout', () => {
    expect(() =>
      render(<ASTextField {...baseProps}  customMinWidth={80} />)
    ).not.toThrow();
  });

  it('renders with defaultValue when field is empty', () => {
    expect(() =>
      render(<ASTextField {...baseProps}  defaultValue="seed" />)
    ).not.toThrow();
  });

  it('renders with maxLength constraint', () => {
    expect(() =>
      render(<ASTextField {...baseProps}  maxLength={50} />)
    ).not.toThrow();
  });

  }
);
